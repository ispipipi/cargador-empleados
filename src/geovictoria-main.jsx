import React from 'react';
import ReactDOM from 'react-dom/client';
import GeovictoriaApp from './GeovictoriaApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GeovictoriaApp />
  </React.StrictMode>,
);
